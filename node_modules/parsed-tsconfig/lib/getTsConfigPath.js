"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const fs_1 = require("fs");
/**
 * Returns the tsconfig path that exists in the current directory
 * @param cwd default `process.cwd()`
 */
function getTsConfigPath(cwd = process.cwd()) {
    const tsconfigPath = (0, path_1.join)(cwd, "tsconfig.json");
    if ((0, fs_1.existsSync)(tsconfigPath)) {
        return tsconfigPath;
    }
    throw new Error("tsconfig file does not exist");
}
exports.default = getTsConfigPath;

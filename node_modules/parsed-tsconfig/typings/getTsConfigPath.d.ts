/**
 * Returns the tsconfig path that exists in the current directory
 * @param cwd default `process.cwd()`
 */
declare function getTsConfigPath(cwd?: string): string;
export default getTsConfigPath;
